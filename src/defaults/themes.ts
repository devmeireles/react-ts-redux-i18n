import { PaletteOptions } from "@mui/material";

export const dark = {
    mode: 'dark',
    background: {
        default: '#030227',
        paper: '#0d0e47'
    }

} as PaletteOptions

export const light = {
    mode: 'light',
    primary: {
        main: '#030227'
    },
} as PaletteOptions
